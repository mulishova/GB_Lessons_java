package Lesson4;

public class LambdaLesson {
    public static void main(String[] args) {
        /*LambdaInter<Integer> function = (x, y) -> x + y;
        System.out.println(function.calculate(1,2));

        CarBuilder carBuilder = Car::new;
        Car car = carBuilder.createCar("BMW");

        LambdaInter<Integer> func = action(1);
        int a = func.calculate(10, 2);
        System.out.println(a);
        int b = action(2).calculate(10, 2);
        System.out.println(b);

        new Car("Subaru")
                .toBeep()
                .start()
                .toBeep()
                .run()
                .toBeep()
                .stop()
                .toBeep();
    }

    private static LambdaInter<Integer> action(int i) {
        switch (i) {
            case 1: return (x, y) -> x * y;
            case 2: return (x, y) -> x / y;
            case 3: return Integer::sum;
            case 4: return (x, y) -> x - y;
            default: return (x, y) -> 0;
        }*/
    }
}
